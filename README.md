# project-qwerty


1. Enter `docker` folder and run `docker compose up -d`
2. Enter container `qwerty-app` and run `composer install`
3. That's it. App will be running on `localhost:8005`

### Install Symfony CLI (optionally)
https://symfony.com/download

https://symfony.com/doc/current/setup.html



.  
.  
.  
App will be running on 
